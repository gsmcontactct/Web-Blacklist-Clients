# Web Blacklist Clients

Aplicatie Flask pentru gestionarea clienților în blacklist.

## Local Run
```bash
pip install -r requirements.txt
python app.py
```

## Deploy pe Render
- Build Command: `pip install -r requirements.txt`
- Start Command: `gunicorn app:app`
